/**
 * Package that contains our primary navigation solutions.
 * 
 * Note that, right now, all of our modules 's classes are contained in the root 
 * documentation(as that was how we originally linked them all) and not in
 * their respective modules or packages.
 *
 * @module @tutorbook/navigation
 * @see {@link https://npmjs.com/package/@tutorbook/navigation}
 * @todo Fix our navigation such that it works with the native browser 
 * navigation buttons (e.g. clicking 'Back' on the browser should be the same as
 * clicking 'Back' within the web app).
 */