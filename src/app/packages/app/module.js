/**
 * Package that contains the primary app class that depends on all of our other
 * app packages and creates an instance of each one depending on the app user's
 * and website's configuration.
 * 
 * Note that, right now, all of our modules 's classes are contained in the root 
 * documentation(as that was how we originally linked them all) and not in
 * their respective modules or packages.
 *
 * @module @tutorbook/app
 * @see {@link https://npmjs.com/package/@tutorbook/app}
 */