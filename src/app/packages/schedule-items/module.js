/**
 * Package that contains all of the schedule item classes (used to populate the
 * [primary schedule view]{@linkplain Schedule}).
 *
 * Note that, right now, all of our modules's classes are contained in the root
 * documentation (as that was how we originally linked them all) and not in
 * their respective modules or packages.
 *
 * @module @tutorbook/schedule-items
 * @see {@link https://npmjs.com/package/@tutorbook/schedule-items}
 */
